---
id: WI-wf-019
gap-id: GAP-wf-020
domain: workflow
status: done
created: "2026-06-03T00:00:00Z"
abandoned-reason: null
closed: "2026-06-03T00:00:00Z"
---

# Work Item: Extend spec-test SKILL.md to create/maintain .tests.json mapping, wire report path, and handle skip convention

**Scope:** `plugin/skills/spec-test/SKILL.md` — add a step (after the existing step 5 "Update the spec item") that creates or updates `.sdd/specs/{domain}/SPEC-{abbrev}.tests.json` with `runner`, `report`, and an `items` map whose entries match the `fullName` substrings linked in each item's `**Tests:**` block; add a step ensuring the declared `report` path is produced by the test runner (e.g. configure Vitest JSON reporter); add a step for writing `**Tests:** skipped — <reason>` for items the skill deliberately skips

**Acceptance criteria:**
- The skill procedure includes a step that writes/updates `.sdd/specs/{domain}/SPEC-{abbrev}.tests.json` with `runner`, `report`, and `items` map keyed by spec item ID
- The `items` map entries use the same substring strings linked in the `**Tests:**` block so `computeTestStatus` resolves them
- The skill procedure includes a step ensuring the declared `report` path is generated by the test runner (e.g. Vitest `--reporter=json` outputting to the declared path)
- The skill procedure includes a step for recording deliberate skips as `**Tests:** skipped — <reason>` in the spec item file rather than leaving the item blank
- Test: `spec-test-skill.test.ts` — SKILL.md contains the `.tests.json` step (mentions `SPEC-{abbrev}.tests.json` or `tests.json`)
- Test: `spec-test-skill.test.ts` — SKILL.md mentions the `report` path / reporter wiring step
- Test: `spec-test-skill.test.ts` — SKILL.md mentions the `skipped` skip convention step
