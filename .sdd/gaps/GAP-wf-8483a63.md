---
id: GAP-wf-8483a63
spec-item: SPEC-wf-042
domain: workflow
status: closed
discovered: "2026-07-03T00:00:00Z"
audit-spec-version: "87bb92ab"
closed-by: WI-wf-0320bed
deferred-reason: null
---

# Gap: optional scope: frontmatter is undocumented and unhandled

**Locations:**
- `plugin/references/schemas.md:98` — the spec item frontmatter block documents `id/domain/abbrev/status/aliases/version` but not the optional `scope:` path-glob field.
- `plugin/references/artifacts/spec.md:18` — the spec artifact guide's required-frontmatter block omits `scope:` and its path-glob semantics.
- `plugin/scripts/spec-index.js` (does not exist) — no script emits scope globs (empty when absent) as SPEC-wf-042 requires.

**Reasoning:** SPEC-wf-042 makes `scope:` an optional, opt-in list of repo-root-relative path globs that discovery and guardian audit treat as authoritative candidate inclusion; the schema reference and spec artifact guide never mention it and no index emits it.
