---
id: TGT-021
status: accepted
created: 2026-05-17
domain: architecture
---

# Target: Targets API includes archived targets

## Current statement
`GET /workspaces/:id/targets` reads `*.md` files from both `targets/` and `targets/archive/` and includes all of them in the JSON response. The frontend uses the `status` field to categorise targets into active and archived sections.

## Dialog
### 2026-05-17 — User
The targets screen archived section never shows anything. Image #4 shows archived targets appearing below a · ARCHIVED N divider. The API currently excludes the archive/ directory so the frontend never gets them.

### 2026-05-17 — Agent
Current statement committed: the endpoint will read from both the active targets directory and the archive subdirectory, returning all targets regardless of status so the frontend can render the archived section.
