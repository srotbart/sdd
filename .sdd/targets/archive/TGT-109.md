---
id: TGT-109
status: accepted
created: 2026-06-05
domain: ui-components
---

# Target: Artifact IDs are clickable links that peek the artifact in a reusable ArtifactPeeker

## Current statement

When an artifact ID (e.g. `TGT-*`, `SPEC-*`, `GAP-*`, `WI-*`, `ISS-*`, `IMP-*`) is shown in
the Hub UI, it is a **clickable link** — except where an ID appears as a title/heading or
other UI chrome, which stays plain. Clicking the link opens the artifact's content in a
container that **slides/expands on top from the right side**. From that viewer the user can
also **navigate directly to the artifact** (its dedicated screen/detail). A reusable
component named **`ArtifactPeeker`** provides the slide-in viewer and is reused everywhere.

1. **Clickable artifact-ID links** — IDs rendered in rows, meta lines, reference pills,
   etc. become links; IDs used as titles/headings or similar chrome are excluded.
2. **Right-side sliding viewer** — clicking opens `ArtifactPeeker`, a panel that expands on
   top from the right and shows the artifact's content.
3. **Navigate-to-artifact** — a control in the peeker jumps directly to that artifact's
   full screen/detail.
4. **Reusable `ArtifactPeeker`** — created once (named exactly `ArtifactPeeker`) and reused
   across the app; works for all artifact types.

Relationship: this is the general form of the Session right-side target panel (SPEC-scr-048).
`ArtifactPeeker` should be the reusable mechanism; existing one-off panels may be aligned to
it where straightforward (without regressing existing behavior/tests).

Open decisions to settle at target-engage (not pinned here):
- Overlay-on-top vs push-content; dismiss affordances (close button / `Esc` / click-outside).
- How the peeker resolves an id → content (reuse per-type detail views where they exist, or
  a generic render) and how it fetches that content.
- Exactly which ID renderings become links vs. count as "title/chrome."
- Whether SPEC-scr-048's Session panel is refactored onto `ArtifactPeeker` now or later.

Scope note: Hub UI; primarily a reusable component (`ui-components`) consumed across screens.
Exact decomposition settled during target-engage.

## Dialog

### 2026-06-05 — User
When an artifact id is shown in the UI (maybe except in UI elements such as titles) it
should be a link I can press to show the content of the artifact in a container that slides
(expands on top) from the right side. It should also allow me to navigate to that artifact
directly in that viewer. A component "ArtifactPeeker" should be created to be reused.

### 2026-06-05 — Agent (engage → accepted)
Decisions (logged): ArtifactPeeker = app-level single instance, overlay-on-top slide from
the right, dismiss via close button + Esc + click-outside; resolves id→content by reusing
per-type detail views where they exist (generic render otherwise); a shared ArtifactIdLink
component linkifies IDs in rows/meta/reference-pills (titles/chrome excluded); "go to
artifact" control navigates to the dedicated screen. SPEC-scr-048's Session panel is NOT
refactored now (left to avoid regressions; may align later). Folded into **SPEC-uic-011**
(component) and **SPEC-uic-012** (ID links). Status → accepted; archived.
