---
id: TGT-017
status: accepted
created: 2026-05-17
domain: ui-screens
---

# Target: Targets screen shows .sdd targets

## Current statement
When the user selects the Targets view in the Hub sidenav, the screen displays the targets loaded from the workspace's `.sdd/targets/` directory.

## Dialog
### 2026-05-17 — User
In the hub when I select the targets view I want to see the targets that are loaded from the .sdd dir.

### 2026-05-17 — Agent
SPEC-scr-003 already specifies the Targets screen layout in detail (two-pane, filter bar, status pills, right-pane detail view). Your target is about data sourcing — connecting that screen to real `.sdd/targets/` files — which is not yet specced.

This points to a missing architecture spec item: a server endpoint analogous to `SPEC-arch-013` (which serves specs as JSON) but for targets.

One question before committing: should this target scope to just the backend API (a new `GET /workspaces/:id/targets` endpoint that reads `.sdd/targets/*.md` excluding `archive/`), or does it also cover something about the screen layout that SPEC-scr-003 doesn't already capture?

If the API is the full scope, I'll add a single item to SPEC-arch.

### 2026-05-17 — User
its about the API

### 2026-05-17 — Agent
Committing. Proposed Current statement:

> The server exposes `GET /workspaces/:id/targets`. It resolves the workspace path from SQLite, reads all `*.md` files from `{path}/.sdd/targets/` (excluding the `archive/` subdirectory), parses frontmatter (`id`, `status`, `created`, `domain`) and the Current statement body, and returns a JSON array of target objects. Returns 404 if the workspace is unknown, empty array if no target files exist.

This maps to a new SPEC-arch-014. Reconciling now.
