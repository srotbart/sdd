---
id: TGT-001
status: accepted
created: 2026-05-15
domain: architecture
---

# Target: Technology stack — Node.js, React, TypeScript, SQLite

## Current statement
Build SDD Hub using Node.js (server), React + TypeScript (frontend), and SQLite (persistence). The stack should support local filesystem watching, WebSocket communication between the hub server and UI clients, and a stdio/WebSocket agent transport for connected Claude Code agents.

## Dialog
### 2026-05-15 — User
Stack to use: Node.js, React, TypeScript, SQLite.

### 2026-05-15 — Agent
The intent is unambiguous. Proposing a Current statement that captures the full stack and its roles:

Proposed Current statement:

> SDD Hub is built on Node.js (server runtime), React + TypeScript (frontend, bundled with Vite), and SQLite (local persistence via better-sqlite3). The server watches `.sdd/` directories with chokidar, serves the UI as static assets, exposes a WebSocket endpoint for real-time state updates to UI clients, and accepts WebSocket connections from Claude Code agents for heartbeats and activity events. No remote services — the hub runs entirely on localhost.

Accept this, or revise and flip status back to awaiting-agent.
