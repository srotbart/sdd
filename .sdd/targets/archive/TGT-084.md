---
id: TGT-084
status: accepted
created: 2026-06-01
domain: ui-screens
---

# Target: Spec list renders item body as markdown, capped at 5 lines

## Current statement

In the spec item list (`SpecItemList`), each item's body is rendered via `react-markdown` rather than as plain text. The rendered content is capped to a maximum of 5 visible lines via CSS `line-clamp`; content beyond 5 lines is clipped. No disclosure affordance ("read more") is shown — clicking the row opens the full detail view.

## Dialog

### 2026-06-01 — User

In the spec list view, each item's body should be rendered as markdown rather than plain text. The rendered content should be limited — up to 5 lines max.

### 2026-06-01 — Agent

Statement is clear. SPEC-scr-037 covers detail view markdown; list view uses plain text — clean Extension. Committing directly.

Proposed Current statement:

> In the spec item list (`SpecItemList`), each item's body is rendered via `react-markdown` rather than as plain text. The rendered content is capped to a maximum of 5 visible lines via CSS `line-clamp`; content beyond 5 lines is clipped. No disclosure affordance ("read more") is shown — clicking the row opens the full detail view.

Outcome: **Extension** — adding SPEC-scr-039.
