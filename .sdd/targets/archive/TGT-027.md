---
id: TGT-027
status: accepted
created: 2026-05-17
domain: ui-screens
---

# Target: Archived divider letter-spacing, font-weight, and collapsible toggle

## Current statement
The archived divider has three remaining gaps versus the design:

1. **Letter-spacing** — design uses `letter-spacing: 0.18em`; current `.targets-archived-divider__label` uses `0.08em`.

2. **Font-weight** — design uses `font-weight: 500` on the label; current has no weight set.

3. **Collapsible behaviour** — in the design the divider is clickable and toggles the archived section open/closed, with a `▾` / `▸` indicator prepended to the label. The current implementation renders the divider as a static non-interactive element.

## Dialog
### 2026-05-17 — User
Archived divider has wrong letter-spacing (0.08em vs 0.18em), missing font-weight: 500, and is not collapsible. Design shows a ▾/▸ toggle. Fix all three in Targets.tsx and Targets.css.
