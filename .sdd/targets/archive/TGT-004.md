---
id: TGT-004
status: accepted
created: 2026-05-15
domain: ui-screens
---

# Target: Implement all UI screens following the design handoff

## Current statement
The SDD Hub UI implements all screens from the editorial design direction defined in `.sdd/specs/design/SDD Hub.html` and the accompanying component files. The approved visual system (white background, charcoal ink, oxblood accent, hairline borders, no shadows, Newsreader/Geist/JetBrains Mono typography, status pills) applies consistently across all screens. The implementation recreates the design in React + TypeScript using the established codebase conventions — not by porting the Babel prototype directly.

## Dialog
### 2026-05-15 — User
Add target to follow the design based on .sdd/specs/design.

### 2026-05-15 — Agent
The design defines 7 screens. Proposing a concrete Current statement that enumerates each one so the spec items are unambiguous:

Proposed Current statement:

> The SDD Hub UI implements all 7 screens from the editorial design direction in `.sdd/specs/design/SDD Hub.html`. Each screen is a React + TypeScript component using the design token CSS already in place. The screens and their scopes:
>
> 1. **Hub Dashboard** (`screens/Dashboard.tsx`) — cross-workspace overview: summary strip (5 counters), workspace tile grid, unified activity stream (last 10 lines).
> 2. **Workspace › Session** (`screens/Session.tsx`) — mirrors `/sdd:session-start` output: pipeline strip, priority-ordered category sections, next-action footer block.
> 3. **Workspace › Targets** (`screens/Targets.tsx`) — two-pane: left filter list, right target detail with dialog and composer.
> 4. **Workspace › Specs** (`screens/Specs.tsx`) — two-pane: left domain list, right spec items with ref pills.
> 5. **Workspace › Gaps** (`screens/Gaps.tsx`) — two-pane: left gap list with domain filter, right gap detail with code context and spec/work-item rail.
> 6. **Workspace › Work Items** (`screens/WorkItems.tsx`) — Kanban (pending / in-progress / blocked / done·today) with side drawer.
> 7. **Workspace › Activity** (`screens/Activity.tsx`) — live log stream with agent filter and live/paused toggle.
>
> The Agents tab present in the prototype is omitted (removed per design README). Settings screen is out of scope for this target. All artifact-ID links route between tabs. The `AgentsView` component in `activity-and-agents.jsx` is not ported.

Accept this, or revise and flip status back to awaiting-agent.
