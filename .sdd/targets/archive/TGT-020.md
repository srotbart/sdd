---
id: TGT-020
status: accepted
created: 2026-05-17
domain: ui-screens
---

# Target: Targets screen shows real data from the backend

## Current statement
When a workspace is active, `client/src/App.tsx` fetches `GET /workspaces/:id/targets` and passes the result to the `Targets` component, replacing the static `MOCK_TARGETS`. The fetch is triggered whenever the active workspace changes. The API response fields (`id`, `status`, `created`, `domain`, `statement`) are mapped to the frontend `Target` type before being passed to the component.

## Dialog
### 2026-05-17 — User
The targets view looks right visually but shows mock data. Wire it to real data.

### 2026-05-17 — Agent
Target is clear — same pattern as the live specs fetch already in App.tsx. Committing.

Proposed Current statement:

> When a workspace is active, `client/src/App.tsx` fetches `GET /workspaces/:id/targets` and passes the result to the `Targets` component, replacing the static `MOCK_TARGETS`. The fetch is triggered whenever the active workspace changes. The API response fields (`id`, `status`, `created`, `domain`, `statement`) are mapped to the frontend `Target` type before being passed to the component.

Reconciling with SPEC-ui-screens now.
