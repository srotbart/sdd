---
id: TGT-089
status: accepted
created: 2026-06-02
domain: ui-screens
---

# Target: Hub Designs screen browses .sdd/design/ directories

## Current statement

Hub backend exposes `GET /workspaces/:id/designs` (list of `{name, lastModified}` for each `.sdd/design/*/design.md`) and `GET /workspaces/:id/designs/:name` (raw markdown). Chokidar watcher extended to `.sdd/design/`. Designs screen added to sidebar between Projections and Gaps: left panel lists designs by last modified, right panel renders selected `design.md` via `react-markdown`, auto-refreshes on `sdd-changed`.

## Dialog

### 2026-06-02 — Agent

Statement clear. Extension → SPEC-scr-042 (backend) and SPEC-scr-043 (frontend).

## Dialog

### 2026-06-02 — User

Add a Designs screen to the Hub that surfaces the .sdd/design/ directory. Each design is a subdirectory with a design.md and optional sibling files. The screen should list designs on the left and render the selected design.md as markdown on the right. Auto-refresh on sdd-changed like Projections.
