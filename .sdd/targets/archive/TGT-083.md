---
id: TGT-083
status: accepted
created: 2026-06-01
domain: ui-screens
---

# Target: Kanban columns fill screen width equally even when empty

## Current statement

The work items kanban board always fills the full width of its containing panel. The four status columns (`pending`, `in progress`, `blocked`, `done · today`) divide the board into equal quarters regardless of how many work items each column contains. A column with no items is the same width as a fully populated column.

## Dialog

### 2026-06-01 — User

The work items kanban columns should fill the width of the screen equally for each column even when no work items exist.

### 2026-06-01 — Agent

Statement is clear. Correcting domain to `ui-screens` (kanban is screen-specific, not a shared component). Committing directly.

Proposed Current statement:

> The work items kanban board always fills the full width of its containing panel. The four status columns (`pending`, `in progress`, `blocked`, `done · today`) divide the board into equal quarters regardless of how many work items each column contains. A column with no items is the same width as a fully populated column.

Outcome: **Extension** — no existing spec item in `ui-screens` covers kanban column width invariant. Adding SPEC-scr-038.
