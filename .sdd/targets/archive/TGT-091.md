---
id: TGT-091
status: accepted
created: 2026-06-02
domain: ui-screens
---

# Target: Plugin reference page derives skill list from installed plugin, not hardcoded array

## Current statement

`GET /plugin-skills` reads `~/.claude/plugins/cache/sdd/sdd/*/skills/*/SKILL.md`, extracts `name` and `description` frontmatter, returns sorted array. `PluginReference.tsx` fetches on mount, replaces hardcoded `SKILLS` constant. Fallback message shown if fetch fails.

## Dialog

### 2026-06-02 — Agent

Clear statement. Plugin path is user-global, not workspace-scoped. Extension → SPEC-scr-044.

## Dialog

### 2026-06-02 — User

The plugin reference page has a hardcoded SKILLS array. It should instead read the available skills from the installed plugin (each skill has a SKILL.md with name and description in frontmatter). The backend should expose a skills endpoint and the frontend should fetch dynamically so new skills appear automatically without code changes.
