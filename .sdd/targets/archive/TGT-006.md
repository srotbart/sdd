---
id: TGT-006
status: accepted
created: 2026-05-15
domain: ui-screens
---

# Target: Settings screen — workspace fields

## Current statement
`client/src/screens/Settings.tsx` renders a workspace settings screen with:

- Page title: `○ workspace settings` in Newsreader italic with a small circle bullet, followed by a hairline separator.
- A **WORKSPACE** section (uppercase eyebrow label + hairline rule).
- A hairline-bordered card containing four rows separated by inner hairlines:
  1. `name` — label on the left, text input on the right showing the workspace name.
  2. `path` — label on the left, text input on the right showing the workspace path.
  3. `description` — label on the left, text input on the right showing the workspace description.
  4. `.sdd location` — label on the left, read-only computed value on the right in muted mono text (no input box), showing `{path}/.sdd`.
- Labels are in `var(--ink-2)`, inputs use hairline borders (`var(--hair-2)`), and the `.sdd location` value is in `var(--ink-3)` mono.
- No save button — changes persist on blur (or are wired to a future API call).

## Dialog
### 2026-05-15 — User
Add target for the settings view to match the screenshot.
