---
id: TGT-077
status: accepted
created: 2026-05-21
domain: ui-screens
---

# Target: SPEC-scr-036 omits 'accepted' from Gaps archivedValues — correct spec text

## Current statement

SPEC-scr-036 states: `archivedValues={['closed','deferred']}` for Gaps.tsx. However SPEC-scr-020 ("Gaps screen fetches live data and shows archived section") defines archived gaps as those with status `closed`, `deferred`, OR `accepted`. The code correctly uses `archivedValues={['closed', 'deferred', 'accepted']}` — consistent with SPEC-scr-020.

The spec text in SPEC-scr-036 must be corrected to include `'accepted'` in the archivedValues list: `archivedValues={['closed','deferred','accepted']}`. This is a spec correction, not a code change.

## Dialog

### 2026-05-21 — User
Discovered via round-4 ephemeral-discovery: investigator 2 found SPEC-scr-036 omits 'accepted' from archivedValues while SPEC-scr-020 and the code both include it. Internal spec inconsistency.

### 2026-05-21 — Agent
Target is clear. Reconciling with spec.
