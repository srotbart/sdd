---
id: TGT-092
status: accepted
created: 2026-06-02
domain: workflow
---

# Target: sdd-worker should be spawned with the sonnet model, not opus

## Current statement

The `sdd:spawn-sdd-worker` skill spawns the `sdd-worker` agent with the Agent tool's `model` parameter explicitly set to `"sonnet"`, rather than inheriting the session model (typically opus). The `model` field is listed alongside `name`, `subagent_type`, `run_in_background`, and `team_name` in the spawn parameters. Rationale: the execution phase (audit → decompose → close) is deterministic given a clear spec, so sonnet is sufficient and cheaper.

## Dialog

### 2026-06-02 — User

The sdd-worker model should be sonnet, not opus. When `/sdd:spawn-sdd-worker` spawns the worker agent (see SPEC-wf-002 / SPEC-wf-005), it currently inherits the session model (opus). The execution phase is deterministic given a clear spec, so the worker should run on sonnet instead.

### 2026-06-02 — Agent

Intent is unambiguous — committing without clarifying questions. The worker is spawned via the Agent tool in `spawn-sdd-worker` (SPEC-wf-002, SPEC-wf-005) with no `model` parameter today, so it inherits the session model. Adding `model: "sonnet"` to the spawn parameters pins it.

Proposed Current statement:

> The `sdd:spawn-sdd-worker` skill spawns the `sdd-worker` agent with the Agent tool's `model` parameter explicitly set to `"sonnet"`, rather than inheriting the session model (typically opus). The `model` field is listed alongside `name`, `subagent_type`, `run_in_background`, and `team_name` in the spawn parameters. Rationale: the execution phase (audit → decompose → close) is deterministic given a clear spec, so sonnet is sufficient and cheaper.

Accept this, or revise and flip status back to awaiting-agent.
