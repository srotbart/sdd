---
id: TGT-061
status: accepted
created: 2026-05-19
domain: ui-screens
---

# Target: MOCK_SPECS removed — specs fetched live from GET /workspaces/:id/specs

## Current statement

`MOCK_SPECS` is still defined in `App.tsx:130` and actively passed to Session, Gaps, WorkItems, and CommandPalette instead of live spec data. TypeScript flags `MOCK_GAPS` and `MOCK_WORK_ITEMS` as unused (those screens now receive live data), but `MOCK_SPECS` remains in use.

The spec data pipeline must be completed:
- `App.tsx` must fetch `GET /workspaces/:id/specs` when a workspace becomes active (same pattern as targets, gaps, work-items)
- `liveSpecs: Spec[]` state must be maintained and passed to Session, Gaps, WorkItems, Specs, and CommandPalette
- `MOCK_SPECS`, `MOCK_GAPS`, and `MOCK_WORK_ITEMS` constants must be deleted (MOCK_GAPS and MOCK_WORK_ITEMS are unused but still defined — remove the dead code)
- The fetch must be triggered by `sdd-changed` WebSocket events with `artifact: "specs"` (already supported by SPEC-arch-018)

## Dialog

### 2026-05-19 — User
Discovered via round-2 ephemeral-discovery: investigators 1 and 4 confirmed MOCK_SPECS is still passed to 4 screens. The specs live-data pipeline was never completed when MOCK_AGENTS/MOCK_WORKSPACES were removed.

### 2026-05-19 — Agent
Target is clear. Reconciling with spec.
