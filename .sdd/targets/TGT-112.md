---
id: TGT-112
status: awaiting-agent
created: 2026-06-05
domain: ui-screens
---

# Target: Comment on selected text in the Projection view

## Current statement

In the Projection view, the user can mark (select) text and attach a comment to it, which is
persisted next to the projection and later addressed (and pruned) by the agent.

1. **Select → marker icon** — when the user selects/marks text in the projection, a small
   icon appears next to the selected text.
2. **Click icon → action menu** — clicking the icon shows these options:
   **clarify**, **re-evaluate**, **expand**, **condense** — with an optional small
   multiline input (a hovering text box) to write a note alongside the chosen action.
3. **Confirm → persist as JSON** — on a confirm action, a JSON file is created/updated **at
   the same location as the projection file**. A new entry is appended containing: the
   **selected text**, the **line**, the **action**, and the user's **input**.
4. **Highlight + type label** — commented text is highlighted, with a **tiny label** (small
   text ~half size, aligned at the end to the top of the last word of the highlighted text)
   indicating which comment type it is.
5. **Hover detail** — hovering over the commented line(s) shows the **date and time** the
   comment was added, including the comment text.
6. **Lifecycle** — once the agent addresses a comment, that entry is **removed** from the
   JSON file.

These behaviors (the four action types, optional multiline note, JSON co-located with the
projection, the entry fields, the tiny end-aligned type label, the hover date/time+comment,
and removal-on-address) are the user's requirements and are pinned.

Open decisions to settle at target-engage (not pinned here):
- **JSON filename & schema** — e.g. `<projection>.comments.json`; exact entry shape and how
  timestamps are recorded.
- **Selection → source mapping** — how a text selection over rendered markdown maps to the
  source line(s)/range (projections render through the shared `Markdown` component).
- **Multiple / overlapping comments** on the same line or range.
- **Server read/write** — endpoint(s) to load and persist the co-located JSON.
- **"Agent addresses it"** — how addressing is triggered and how entries are pruned (likely a
  skill that reads the comments file, acts, and removes handled entries).

Scope note: primarily the Projections screen (`ui-screens`); also needs a Hub server
read/write for the co-located JSON and an agent workflow to address + prune comments. Exact
decomposition settled during target-engage.

## Dialog

### 2026-06-05 — User
Allow the user to mark text in the projection view and comment on it. Once marked, a small
icon appears next to the selected text; clicking it shows options — clarify, re-evaluate,
expand, condense — with an optional small multiline hovering input. On confirm, create a JSON
file at the same place as the projection, adding an entry with the selected text, the line,
the action, and the user input. Commented text is highlighted with a tiny label (half-size,
aligned at the end to the top of the last word of the highlighted text) indicating the
comment type. Hovering over commented line(s) shows the date/time added and the comment. Once
the agent addresses the comments, they are removed from the JSON file.
