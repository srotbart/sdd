---
id: TGT-124
status: awaiting-user
created: 2026-06-27
domain: workflow
---

# Target: Update the README and add a project roadmap

## Current statement

The project's **README** should be brought up to date so it accurately reflects the current
state of the SDD plugin — its skills, pipeline, and how to get started — and a **roadmap**
should be added so there is a single place that communicates where the project is heading.

It must:
- **Refresh the README** so it agrees with the current set of skills, the SDD pipeline model,
  and the actual install/usage flow — no stale or missing entries (cf. the drift surfaces in
  [[TGT-122]], e.g. the auto-generated "## Skills" table).
- **Add a roadmap** that lays out planned/in-flight direction at a useful granularity —
  enough to orient a reader on what is done, what is next, and what is aspirational.
- Keep both **honest and current**: the roadmap reflects real intent (north star / targets,
  cf. [[TGT-121]]), not wishlist filler, and the README does not overstate what exists.

Settled decisions (2026-07-03):
- **Roadmap location & form** — top-level `ROADMAP.md`, prose grouped as
  Shipped / In Flight / Later, linked from a `## Roadmap` section in the README.
- **Roadmap source of truth** — derived: "In flight" mirrors active `.sdd/targets/`,
  "Shipped" mirrors accepted targets and git history, "Later" mirrors `.sdd/later.md` and
  open-decision targets. Hand-rendered for now; an automated drift check is deferred to
  [[TGT-122]] (the roadmap is itself an entry for the drift registry).
- **README scope** — full refresh: every prose section audited against current behaviour;
  the generated "## Skills" table kept verbatim (generator/check unchanged).
- **Generality** — this repo only; a reusable roadmap pattern for any SDD project can
  become its own target if wanted.

## Dialog

### 2026-06-27 — User
Update readme + add roadmap.

### 2026-07-03 — Agent
Implemented directly per your session request ("let's work on updating the readme with a
roadmap"). The four open decisions were settled with the defaults recorded above (the
interactive question UI was unavailable this session — push back on any of them and I'll
adjust). What shipped on branch `claude/heu-mhlmue`:

- **README full refresh**: added a repo-layout section (plugin/hub/.sdd) and a Hub section;
  pipeline diagram now includes the review loop and worker handoff; artifact table extended
  to all six types plus supporting dirs (design/standards/projections); archiving section
  rewritten for the archive-slimming model (history-as-archive, local-only ephemeral
  archives, hash IDs); fixed `/sdd:init` → `/sdd:sdd-init` and the broken
  `references/…` links (now `plugin/references/…`); corrected "specs never archive" (they
  archive on deprecate/alias, are never deleted); terminal-states table now covers spec
  items and issues/improvements; design-decisions section updated (hash IDs, standards
  layers, history-as-archive). Skills table untouched; `check-skills-drift.js` passes.
- **ROADMAP.md**: Shipped / In Flight / Later, derived as described above, with a
  maintenance rule in the header.

If the decisions and content look right, flip to `ready` (or `accepted` once merged);
if not, note what to change and flip back to `awaiting-agent`.
