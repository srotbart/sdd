---
id: TGT-129
status: draft
created: 2026-07-03
domain: workflow
---

# Target: Coding conventions become spec items in a conventions domain

## Current statement

**PARKED — follow up after worker-v2 (PR #21) has baked in real use.**

Retire the separate `.sdd/standards/` artifact and express coding conventions
as ordinary spec items in a new `conventions` domain (abbrev `conv`). No new
machinery is required: worker-v2's scoped discovery (spec index + `scope:`
globs, SPEC-wf-039/040/042) already loads the right spec items for the code
being touched — conventions simply become items that exist, carrying `scope:`.

Settled leanings (to re-validate at engage):
- Conventions stay user-authored intent, negotiated via targets.
- Items must pass the normal spec bar (verifiable Invariant + Acceptance
  criteria); `scope:` mandatory in this domain; fully auditable like any
  domain.
- Mechanical rules (indent, const/var, import order) belong to linters, not
  spec items.
- At engage: create the domain, author the initial items from the current
  standards doc, retire `.sdd/standards/`, amend SPEC-wf-028 (standards live
  outside the spec — inverted) and SPEC-wf-029 (enforcement layers — layer 1
  becomes discovery), rewire CLAUDE.md + the review-issues rubric.

## Dialog

### 2026-07-03 — User
Raised as: conventions/style the worker should follow; reuse specs with a
conventions domain rather than a separate artifact. An elaborate design
(migration table, per-rule bucketing) was drafted and deliberately discarded
as over-engineering — the scoped-spec mechanism suffices as-is. Parked per
iteration philosophy; flip to awaiting-agent once worker-v2 has run a real
close-domain cycle.
